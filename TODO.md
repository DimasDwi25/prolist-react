# TODO: Display Relation Information in PackingListPage.jsx

- [x] Update fetchPackingLists function to include destination_id, expedition_id, and pl_type_id in the rows mapping.
- [x] Add new columns for DESTINATION_ID, EXPEDITION_ID, and PL_TYPE_ID in the columns array after their respective name columns.
- [x] Remove the ID columns as per user feedback, keeping only the name columns.
- [x] Fix destination display to show name instead of [object Object].
- [x] Fix pl_type to display properly instead of not showing.
- [x] Create maps for destinations, expeditions, and pl_types to display names based on IDs.
- [x] Adjust to use the correct API response structure for destination, expedition, and pl_type names.
